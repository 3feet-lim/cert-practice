# Terraform boundary

Infrastructure provisioning is intentionally deferred. This directory contains no executable Terraform configuration and must not be used to initialize, plan, apply, or inspect AWS resources until the real-integration gate has explicit user approval.
